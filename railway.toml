[deploy]
startCommand = "gunicorn app:app --worker-class gthread --timeout 900 --workers 2 --threads 12"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
